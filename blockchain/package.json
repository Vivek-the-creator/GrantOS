{
  "name": "grantos-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "GrantOS Smart Contracts & MST Blockchain Toolchain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.5",
    "typescript": "^5.3.3"
  }
}
